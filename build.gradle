allprojects {
    apply plugin: 'java'
    apply plugin: 'maven'
    apply plugin: 'idea'

    repositories {
        mavenLocal()
        mavenCentral()
    }
}

idea.project.languageLevel = '1.6'

task wrapper(type: Wrapper) {
    gradleVersion = '1.0'
}